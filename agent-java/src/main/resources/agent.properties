# QLTZ Agent 内置编译配置 — maven resource filtering 会在构建时替换
# 通过 mvn package -Dbuild.server=https://your-server.com -Dbuild.token=YOUR_TOKEN 注入
server=${build.server}
token=${build.token}
interval=${build.interval}
log_level=${build.log_level}
skip_conn=${build.skip_conn}
skip_procs=${build.skip_procs}
